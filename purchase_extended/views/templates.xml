<odoo>
    <data>


<!-- 
        <record id="send_reminder_mail_mail" model="mail.template">
            <field name="name">products rating reminder mail</field>
            <field name="model_id" ref="product.model_product_template"></field>
            <field name="email_from"><![CDATA[${object.login}]]></field>
            <field name="subject">Set Rating for Products reminder </field>
            <field name="auto_delete" eval="False"/>
            <field name="built_in" eval="True"/>
            <field name="body_html" type="html">
                <p>Hi,</p>
              <p>Please set reting for unrated products</p>
           
            </field>
        </record> -->
        
        
        <record id="send_reminder_mail" model="mail.template">
            <field name="name">products rating reminder mail</field>
            <field name="model_id" ref="product.model_product_template"/>
                        <field name="subject">Set Rating for Products reminder </field>

<!--             <field name="partner_to">${object.partner_id != False and object.partner_id.id}</field> -->
<!--             <field name="email_to">${(not object.partner_id and object.email_from)|safe}</field> -->
            <field name="body_html"></field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="True"/>
        </record>

        
        <record id="send_reminder_mail_cron" model="ir.cron">
            <field name="name">products rating</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">model.send_remainder()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">weeks</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True" />
            <field name="doall" eval="True" />
        </record>
    </data>
</odoo>
